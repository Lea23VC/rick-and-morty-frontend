import { responsiveSizes } from "../ts/types/props.types";

export function gridOptions(
  xs: responsiveSizes,
  sm: responsiveSizes,
  md: responsiveSizes,
  lg: responsiveSizes
) {
  return {
    xs: xs,
    sm: sm,
    md: md,
    lg: lg,
  };
}
